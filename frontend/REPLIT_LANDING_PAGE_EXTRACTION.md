# Prompt to Extract Landing Page Code from Replit

Copy and paste this into Replit to extract the exact landing page code:

---

**PROMPT FOR REPLIT:**

```
I need to extract the exact code for the landing/empty state page of the LLM chat interface. This is the initial view shown when there are no messages yet.

Please provide the complete code for:

1. **Main Landing Page Component/Container:**
   - The container that wraps the entire landing page content
   - Layout structure (flexbox/grid)
   - Vertical centering implementation
   - Background colors and styling
   - Width constraints and spacing

2. **Vorta Icon:**
   - The blue asterisk/star icon component
   - Size (should be around 48px)
   - Glow/filter effects
   - Any animations
   - Exact positioning and margins

3. **Title Text:**
   - The main heading text ("What can I help you with?" or similar)
   - Font size, weight, color
   - Typography styling
   - Margins and spacing

4. **Subtitle Text:**
   - The descriptive text below the title
   - Font size, weight, color
   - Typography styling
   - Margins and spacing

5. **Input Container:**
   - The input field container/wrapper
   - Background color (#202A37 or exact value)
   - Border styling (1px solid rgba(255,255,255,0.08) or exact)
   - Border radius (12px or exact)
   - Padding values
   - Max-width constraint (520px or exact)
   - Horizontal centering

6. **Input Field (Text Area):**
   - The actual text input/textarea element
   - Placeholder text ("Ask anything about your business..." or exact)
   - Font styling (size, color, family)
   - Background (transparent)
   - Border and outline styles
   - Padding and spacing

7. **Left-Side Icons (Inside Input):**
   - The icons on the left side of the input (Globe, Paperclip, Mic - exactly 3 icons)
   - Icon sizes
   - Icon colors
   - Spacing between icons (gap)
   - Button styling (transparent background, borders, hover states)
   - Exact icon components used

8. **Send Button (Right Side):**
   - The circular/rounded button on the right
   - Background color (blue #5B9EFF or exact)
   - Size (width/height)
   - Border radius
   - Arrow icon inside
   - Hover states
   - Disabled states
   - Animation/transitions

9. **"Get Started" Section:**
   - The container for the "Get Started" section
   - Title with close button (X icon)
   - Title styling
   - Close button styling and positioning
   - Margins and spacing

10. **Get Started Cards:**
    - The three horizontal cards container
    - Layout (flexbox with gap)
    - Each card:
      - Background color (#202A37 or exact)
      - Border (1px solid rgba(255,255,255,0.06) or exact)
      - Border radius (12px or exact)
      - Padding
      - Width/flex properties
      - Icon at the top (Forecast, Generate Reports, Create Tool)
      - Icon sizes and colors
      - Title text styling
      - Description text styling
      - Hover effects (border color changes, no background change)
      - Click handlers

11. **Overall Layout Structure:**
    - How everything is positioned relative to each other
    - Vertical spacing between elements
    - Horizontal alignment
    - Responsive behavior

Please provide:
- Complete component code with all imports
- All inline styles or CSS classes
- Exact color values (hex, rgba, etc.)
- Exact spacing values (px, rem, etc.)
- All animations and transitions
- Complete file paths for each component

Show me the complete, working code - not pseudocode. Include all styling, animations, and interactive states.
```

---

**ALTERNATIVE SHORTER PROMPT (if the above doesn't work):**

```
Extract the complete code for the LLM chat landing page - the empty state that shows when there are no messages yet.

I need:
1. The main container component
2. Vorta icon (blue asterisk, ~48px)
3. Title and subtitle text
4. Input container with 3 left icons (Globe, Paperclip, Mic) and send button
5. "Get Started" section with 3 cards (Forecast, Generate Reports, Create Tool)
6. All styling, spacing, colors, and animations

Provide complete code with all imports and exact styling values.
```
